g++ -O2 -std=c++17 main.cpp tsp.cpp algorithms.cpp neighbourhood.cpp -o tsp.exe && ./tsp.exe > wyniki2.csv

`wyniki2.csv` zawiera tabele wypisywane na standardowe wyjscie dla zadania 2.
Program zapisuje wyniki zadania 3 osobno do `wyniki3.csv`.

Wklejać zawartości plików tekstowych z wynikami do solvera, do kolumny F, od wiersza 3 w dół.
